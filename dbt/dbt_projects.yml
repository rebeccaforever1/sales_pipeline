name: sales_pipeline
version: 1.0.0
config-version: 2

profile: sales_pipeline

model-paths: ["models"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_modules"

models:
  sales_pipeline:
    staging:
      +materialized: view
    marts:
      +materialized: table